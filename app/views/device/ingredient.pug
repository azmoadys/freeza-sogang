doctype html
html(lang='en')
  include ../common/head.pug
  +head("Freeza - By Time")
  body
    header
      include ../common/navbar.pug
      +navbar(user)
      .container
        .row.align-items-center
          .col-md-6.order-md-1.text-center.text-md-left.pr-md-5
            h1.mb-3 Register
            p.lead Please enter your device's SID.
						/*
            form(action="/register_sensor", method="POST")
              .input-group.mb-3
                input(name="SID", type="integer", placeholder="Enter your SID (Sensor ID)").form-control
                .input-group-append
                  button(type="submit").btn.btn-primary Submit
									*/
    include ../common/footer.pug
    +footer()
